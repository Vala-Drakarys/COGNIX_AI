// Deterministic foundation adapter. The production controller remains authoritative.
export function classifyFoundationResponse(hypothesisId,response){const n=String(response).trim().toLowerCase();if(hypothesisId==='H1'||hypothesisId==='concept'){return /f\s*=\s*m\s*a|f=m\*a/.test(n)?{result:'CONTRADICTS',note:'Student correctly recalled F = ma.'}:{result:'SUPPORTS',note:'Student did not demonstrate recall of F = ma.'};}if(hypothesisId==='H2'||hypothesisId==='calculation'){return ['5','5 n/kg','5 n/kg.'].includes(n)?{result:'CONTRADICTS',note:'Student correctly calculated 10 ÷ 2.'}:{result:'SUPPORTS',note:'Student struggled with the numerical application.'};}return {result:'INSUFFICIENT',note:'No deterministic foundation rule matched this response.'};}
